import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionic.TikStar',
  appName: 'buugui',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
